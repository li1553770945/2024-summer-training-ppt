---
# You can also start simply with 'default'
theme: default
# random image from a curated Unsplash collection by Anthony
# like them? see https://unsplash.com/collections/94734566/slidev
# some information about your slides (markdown enabled)
title: "讲题"
# apply unocss classes to the current slide
class: text-center

# https://sli.dev/features/drawing
drawings:
  persist: false
# slide transition: https://sli.dev/guide/animations.html#slide-transitions
transition: slide-left
# enable MDC Syntax: https://sli.dev/features/mdc
mdc: true
---

# 0122 讲题

---

## 带符号的难度

<br>


### 题意

输入一个实数 $X.Y$,输出根据 $Y$ 的值对 $X$ 做出的判断，格式为 $X$ 后紧跟符号 `-`、空白或 `+`。

### 解法

首先使用double类型输入实数$X.Y$，然后通过类型转换成int，完成取整操作提取出整数部分$X$，再用$X.Y-X$得到小数部分的第一位 $Y$。
---

## 同名

<br>


### 题意

给定$N$个人，每个人有一个姓$S_i$和名$T_i$，判断是否存在两个人具有相同的姓和名。


### 解法

为了判断是否有重复的姓和名，可以利用哈希表（unordered_set）来存储每个人的姓和名的组合。由于名和姓均不包含符合，因此可以使用空格拼接每个人的姓和名形成一个唯一标识符，然后判断该标识符是否已经出现过。如果出现过，说明存在重复，直接输出Yes；如果遍历完所有人都没有重复，输出No。

---

## 逆置换

<br>


### 题意

给定一个长度为 $N$ 的置换 $P = (p_1, p_2, \dots, p_N)$，其中每个数字 $1, 2, \dots, N$ 恰好出现一次，要求构造一个长度为 $N$ 的置换 $Q = (q_1, q_2, \dots, q_N)$，使得对于每个 $i$，有 $q_{p_i} = i$，即对于每个位置 $p_i$，置换 $Q$ 中第 $p_i$ 个元素是 $i$。

### 解法

我们要求的是置换 $Q$，且对于每个 $i$，$q_{p_i} = i$。也就是说，$P$ 中的每个元素 $p_i$ 给出了置换 $Q$ 中的一个位置，而 $Q$ 中该位置应当放置数字 $i$。这就是一个反向映射的问题。


1. 读入置换 $P$。
2. 对于每个 $i$，通过 $p_i$ 找到位置，将数字 $i$ 存放到置换 $Q$ 中的相应位置，即 $q_{p_i} = i$。
3. 输出置换 $Q$。

---

## PairofBalls

<br>


### 题意

有$m$个栈，每个栈里有$k_i$个数字，总共有2n个数字，每个数字出现两次。

每次可以选择两个相同的栈顶数字消掉，能否消掉全部数字？

### 解法

每个圆柱体中的球是以栈的形式存储的，因此我们可以从栈顶移除球，并尝试配对。

从第一个圆柱体开始，检查栈顶球的颜色，如果在其他圆柱体中也能找到相同颜色的球，就移除这两个球，并继续处理其他圆柱体。每次配对成功后，递归调用处理函数。

为了优化找球的过程，可以定义一个`num2index`映射，表示把一个颜色映射到当前顶部是`num`颜色的圆柱的索引，检查是否已经有相同颜色的时候，只要检查`num2index[num]`即可。

如果所有颜色的球都能配对移除，最终配对数应该为$N$，否则返回"No"。


---

## 排序查询

<br>


### 题意

给定一个空序列 $A$，进行 $Q$ 个查询，查询类型有三种：

1. 将整数 $x$ 追加到序列 $A$ 的末尾。
2. 输出序列 $A$ 的第一个元素，并删除它。
3. 将序列 $A$ 按升序排序。
对于查询类型为2的查询，输出序列的第一个元素。

### 解法

容易想到的是，直接完全模拟三个操作，能获得部分分数。


这种方法的时间复杂度在极端情况下是$n^2 \log n$的，因为每次排序我们都是真的排了，但事实上不需要，使用一个普通队列和一个优先队列优化即可。

---

对于1操作，只需要将x放入普通队列中即可。

对于3操作，只需要将普通队列中的所有数取出来都放入优先队列中即可，优先队列可以自动排序。

而对于2操作，若优先队列不为空，则输出优先队列开头的元素，反之则输出普通队列开头的元素。因为如果优先队列不为空，则必然执行过3操作，此时原题中的队列开头的元素必为优先队列开头的元素，而普通队列中的元素都被插入在优先队列元素的后面了。

---

## 链式参赛者

<br>


### 题意
有$n$场、$m$种比赛，种类最多有10种，现给定比赛举办的序列，要求如果要参加相同种类的比赛只能连续参加，问有多少种参加比赛的方案。

### 解法

### 暴力

可以枚举每种比赛选或者不选，时间复杂度为$2^n$。

考虑使用状压dp。

设第$i$场比赛的种类为$s[i]$。第$i$场比赛是否参加，取决于之前的比赛。如果之前的比赛已参加过$s[i]$种类，那么这次就不能参加，除非上一次参加的比赛也是$s[i]$种类（即连续参加相同比赛），这样可以接着参加。

---

### 状态转移设计

我们需要记录的状态有：  
- 上一次参加的比赛种类
- 之前参加过的所有比赛种类

由于比赛种类较少，可以用状态压缩，使用二进制来记录，每一位二进制数的值代表是否参加过该种类比赛。

定义DP数组`f[i][j][k]`表示前$i$场比赛的方案数，条件如下：
- `j`：在二进制下，表示前$i$场比赛中打过的比赛种类。`j`是一个二进制数，每一位代表是否参加过对应的比赛种类。
- `k`：表示第$i$场比赛的种类。

---

## 状态转移方程

考虑第$i$场比赛是否参加，若要参加，首先要保证以下条件：
- 上一场比赛种类与当前比赛种类一致（如果连续参加相同种类的比赛）
- 如果当前比赛种类和之前的种类不同，则需要保证该种类之前没有被参加过。


  
---

## 初始条件

初始化条件为：

第一场比赛，任意比赛种类都可以选择，且不会受到之前比赛的限制。$f[1][1 << s[1]][s[1]] = 1$

其中，`s[1]`表示第一场比赛的种类，且比赛种类在二进制下的位置为`1 << s[1]`。

## 结果

最终的结果是所有满足条件的方案数，可以通过累加所有符合要求的`f[n][j][k]`来得到：

$$
\text{result} = \sum_{j} \sum_{k} f[n][j][k]
$$

### 时间复杂度

由于有$O(n)$场比赛，比赛种类数为$m$，对于每场比赛，我们需要遍历$2^m$种状态，因此时间复杂度为：$O(n \times 2^m)$

