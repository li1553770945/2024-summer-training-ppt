---
# You can also start simply with 'default'
theme: default
# random image from a curated Unsplash collection by Anthony
# like them? see https://unsplash.com/collections/94734566/slidev
# some information about your slides (markdown enabled)
title: "讲题"
# apply unocss classes to the current slide
class: text-center

# https://sli.dev/features/drawing
drawings:
  persist: false
# slide transition: https://sli.dev/guide/animations.html#slide-transitions
transition: slide-left
# enable MDC Syntax: https://sli.dev/features/mdc
mdc: true
---

# 0120 讲题

---

## [ABC215-A]你的第一个评判

<br>


### 题意

给定一个字符串$S$，判断其是否与字符串"Hello,World!"完全匹配。如果完全匹配，输出"AC"，否则输出"WA"。完全匹配指的是两个字符串长度相同且每个位置的字符都相同。

### 解法

1. 读取输入字符串$S$。
2. 定义目标字符串为"Hello,World!"。
3. 使用循环逐个比较$S$与目标字符串的每个字符：
   - 如果发现任何字符不匹配，输出"WA"并结束程序。
   - 如果所有字符都匹配，输出"AC"。
4. 判断完所有字符后，若没有返回"WA"说明完全匹配，输出"AC"。

---

## $\log_2(N)$

<br>


### 题意

给定一个正整数$N$，要求找到一个最大的整数$k$，使得$2^k \leq N$。


### 解法

本题的核心是找到最大的整数$k$，使得$2^k$不超过给定的$N$。由于$2^k$是指数增长的，我们只需要不断计算$2^k$，直到找到最大满足条件的$k$。

从$k=0$开始，计算$2^k$，并逐步增加$k$，直到$2^k > N$。
当$2^k > N$时，输出$k-1$，因为在循环结束时$k$已经超出了最大符合条件的$k$值。

---

## 再多一个aabababaa

<br>


### 题意

给定一个字符串$S$和一个整数$K$，要求找出字符串$S$的所有排列中字典序第$K$小的排列。字符串$S$中可能包含重复字符，要求返回第$K$个排列。



### 解法

我们需要找出字符串的所有排列，并按照字典序排序。可以先对字符串进行排序，以确保排列从字典序的最小排列开始。

### 递归生成全排列

我们可以利用递归的方式来生成所有排列，并在递归过程中通过剪枝跳过重复字符，避免生成重复的排列。
递归的过程中，通过控制字符选择顺序和使用标记数组used[]来确保每个字符只能被选中一次。

---

### 使用next_permutation

next_permutation 是 C++ 标准库中的一个算法，用于生成当前排列的字典序下一个排列。它位于 `<algorithm>` 头文件中，通常用来生成所有可能的排列。

```cpp
template <class BidirIt>
bool next_permutation(BidirIt first, BidirIt last);
```

### 参数说明
first：输入序列的第一个元素的迭代器。

last：输入序列的末尾后一个元素的迭代器。

### 返回值
返回 true：如果存在下一个字典序排列，且排列已被修改为下一个排列。

返回 false：如果已经生成了最大的字典序排列，当前排列不会被修改，函数返回 false。

---

## 互质2

<br>


### 题意

给定数列 $A_N$ 和一个正整数 $M$，求出所有的 $1 \leq k \leq M$ 满足 $\forall i \in [1, N], \gcd(k, A_i) = 1$。

### 解法

本题的关键在于通过质因数分解来筛选符合条件的整数 $k$。如果一个整数 $k$ 和某个 $A_i$ 不互质，那么 $k$ 必定含有 $A_i$ 的某个质因数。因此，判断一个整数 $k$ 是否符合条件，可以通过先找到所有 $A_i$ 的质因数，之后筛掉含有这些质因数的 $k$。

1. **质因数分解**：首先对序列 $A$ 中的每个元素进行质因数分解，得到其所有的质因数。
2. **输出符合条件的整数**：遍历 $1 \leq k \leq M$，对$k$进行质因数分解，输出所有不含上面得到的质因数集合的整数。
  
[线性复杂度解法-供参考](https://www.cnblogs.com/User-Unauthorized/p/solution-AT-ABC215-D.html)

---


## 游乐园

<br>


### 题意

有N个游乐设施，每个设施有初始的乐趣值，当高桥乘坐某个设施时，乐趣值会减少1。高桥总共最多可以乘坐K次游乐设施，每个设施可以多次乘坐。问高桥如何选择设施，能够使得他的总满意度最大化。

### 解法

首先，高桥每次乘坐某个设施都会增加其乐趣值，但乘坐后该设施的乐趣值会减1。因此，最佳的策略是：每次选择当前乐趣值最大的设施进行乘坐。

为了实现这一点，可以使用一个优先队列（大根堆）来存储当前所有设施的乐趣值，每次从队列中取出最大值并将其减去1，直到达到K次乘坐的限制。但是这样会超时，因此我们可以想一个办法来优化。

我们首先对所有的娱乐设施按照快乐值从大到小排序，我们肯定要乘坐最大的，直到最大的和次大的想等。 接下来，我们轮流做最大的和次大的，直到和第三大的相等，然后轮流做前三个，以此类推。

先不考虑k的影响，我们的过程就变为：遍历所有的娱乐设施，对于每个娱乐设施，能获得的快乐值为（当前快乐值-下一个设施的快乐值+1） $\times$ 前面的设施数量（包括当前的）。然后加上k之后，特判处理一下即可。 

---

## 最大距离2

<br>


### 题意

给定$N$个在二维平面上的不同点，每个点有$x_i$和$y_i$两个坐标，定义两点$i$和$j$之间的距离为$min(|x_i - x_j|, |y_i - y_j|)$，即两个点在$x$和$y$坐标差中的较小值。要求找出两点之间的最大距离。



### 解法

### 思路

暴力解法即直接计算所有点对之间的距离，时间复杂度为$O(N^2)$，对于最大$N = 200000$的情况，无法在规定的时间内完成。

通过二分法来优化。我们可以通过对点按$x$坐标排序，保持有序性来减小计算的复杂度。同时，通过维护当前点的$y$坐标的最小值和最大值，使用双指针来快速判断是否存在满足条件的点对。


---

### 关键步骤

1. 排序：我们先对所有的点按$x$坐标进行排序。这样做的好处是，相同的$x$坐标的点是邻近的，且$x$坐标差异大的点一定在排序后的序列中距离较远。

2. 二分查找：我们要找到最大距离，所以可以通过二分法来逐步尝试不同的距离值$mid$，看看是否存在满足条件的点对。具体来说，二分查找的过程如下：

设定一个初始的距离范围[0, 2e9]，即距离可能的最大值。
每次取中值$mid$，然后判断是否存在两点之间的距离大于等于$mid$。
如果存在，说明我们可以尝试更大的距离，将搜索范围缩小到$[mid, 2e9]$；否则，将搜索范围缩小到$[0, mid-1]$。

---


3. 双指针：判断是否存在两点之间的距离大于等于$mid$的核心思想是使用双指针。我们遍历所有点$i$，对于每一个点$i$，首先遍历找到$x_j-x_i > mid$的位置，除了$x$要满足，我们还需要保证$y$也满足。
   
4. 后缀最小值与最大值：为了高效地判断两个点之间的$y$坐标差是否满足条件，我们可以通过动态维护一个后缀的最小值和最大值数组来实现：

mi[i]表示从点$i$到点$N$之间，$y$坐标的最小值。
mx[i]表示从点$i$到点$N$之间，$y$坐标的最大值。

5. 继续双指针同时判断$x$和$y$:已知$j$后面的所有点，与$i$的横坐标都大于mid，因此只需要判断后面是否有满足$y$条件的点即可，即$|ma[j] - y_i| \geq mid$或者$|mi[j] - y_i| \geq mid$。
   
如果存在满足的，则直接返回真，否则继续遍历下一个$i$，并找到新的$x$坐标满足要求的$j$，再判断$y$。

---

## 

<br>


### 题意


### 解法
