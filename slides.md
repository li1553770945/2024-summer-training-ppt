---
# You can also start simply with 'default'
theme: default
# random image from a curated Unsplash collection by Anthony
# like them? see https://unsplash.com/collections/94734566/slidev
# some information about your slides (markdown enabled)
title: "讲题"
# apply unocss classes to the current slide
class: text-center

# https://sli.dev/features/drawing
drawings:
  persist: false
# slide transition: https://sli.dev/guide/animations.html#slide-transitions
transition: slide-left
# enable MDC Syntax: https://sli.dev/features/mdc
mdc: true
---

# 0124 讲题

---

## 字典序

<br>


### 题意

给定两个不同的字符串 $S$ 和 $T$，判断 $S$ 是否在字典序上小于 $T$。如果是，输出 `Yes`，否则输出 `No`。

### 解法

由于题目中已经说明 $S$ 和 $T$ 是不同的字符串，因此我们只需要按照字典序的规则进行比较即可。在C++中，string类型可以直接进行比较，如果是C语言，需要使用strcmp函数。


---

## AtCoder测验

<br>


### 题意

给定三个不同的字符串 $S_1$、$S_2$ 和 $S_3$，它们分别属于集合 $\{\text{ABC}, \text{ARC}, \text{AGC}, \text{AHC}\}$。找出集合中未被给定的那个字符串。

### 解法

题目要求从四个固定的字符串中找出未被输入的字符串。可以通过标记已输入的字符串，然后遍历所有可能的字符串，找到未被标记的那个。


---

## Shapes

<br>


### 题意

给定两个二维图形$S$和$T$，判断能否通过将$S$旋转$90^\circ$的整数倍（$0^\circ,90^\circ,180^\circ,270^\circ$）后平移，使其与$T$完全重合。

### 解法

#### 思路

本题数据范围较小，因此可以直接模拟整个过程暴力匹配。

图形匹配的关键在于**形状结构相同**，与绝对位置无关。通过以下步骤判断：
1. 穷举所有可能的旋转角度（4种）
2. 比较$S$与$T$的结构是否完全一致

---

在比较过程中，如果使用暴力枚举所有平移的可能，则时间复杂度是$n^2$,如果在进行一一匹配，时间复杂度也是$n^2$，总时间复杂度为$n^4$会超时。

我们需要知道两个图形能否仅通过平移得到对方，因此，我们只要找到两个图形的同一个参照点，然后计算出参照点差了多少，使用这个参照点的差距当做是平移距离即可。

在本题中，我们可以计算两个图形最靠上靠左的那个点，即可算出两个图形平移后的差距。 

需要注意的是，比较的时候需要双向比较，如果S中有#，保证平移后T对应的位置也是#。反之，如果T中有#，保证平移后S对应的位置也是#。


---

## 切割木头

<br>


### 题意

我们有一个长度为 $L$ 的木头，初始时没有切割。接下来有 $Q$ 个查询，每个查询有两种类型：

1. 在位置 $x$ 处将木头切割成两段。
2. 查询包含位置 $x$ 的那段木头的长度。

要求按顺序处理所有查询，并输出所有类型为 2 的查询的结果。


### 解法

为了高效处理查询，我们需要快速找到某个位置所在的木头的长度。由于木头会被多次切割，我们可以使用一个有序的数据结构来记录所有切割点的位置。这样，对于每个查询，我们可以通过二分查找快速定位到包含该位置的木头的左右端点，从而计算出长度。

---

#### 关键步骤

1. **初始化**：我们使用一个 `set` 来存储所有切割点的位置。初始时，`set` 中包含木头的起点 $0$ 和终点 $L$。
  
2. **处理查询**：
   - 对于类型为 1 的查询（切割操作），我们只需要将切割点 $x$ 插入到 `set` 中。
   - 对于类型为 2 的查询（查询操作），我们使用 `upper_bound` 找到第一个大于等于 $x$ 的切割点，这个点就是包含 $x$ 的木头的右端点。然后通过左移指针找到左端点，最后计算两个端点之间的差值，即为所求的木头的长度。

3. **输出结果**：对于每个类型为 2 的查询，输出计算得到的长度。
   
---

`upper_bound` 用于查找容器中第一个**大于**给定值的元素的位置。如果容器中有多个元素等于给定值，它将返回第一个大于该值的元素位置。

- **函数原型**：
    ```cpp
    iterator upper_bound(iterator first, iterator last, const T& val);
    ```

- **参数**：
    - `first`：容器的起始迭代器。
    - `last`：容器的结束迭代器。
    - `val`：需要查找的值。

- **返回值**：返回一个迭代器，指向第一个**大于** `val` 的元素。如果所有元素都小于或等于 `val`，则返回 `last`。


---

## 破坏

<br>


### 题意

给定一个无向图，边权有正有负，从中选出几条边，要求选出的边权总和最大并且剩下的图要是一个连通图，输出最大的边权。

### 解法

最终保留的边必须构成生成树。要使移除的正权边总和最大，等价于生成树中保留的正权边总和最小。将边权处理为 $\max(C_i, 0)$，构造最小生成树：此时负权边权值视为0（必须保留），正权边按原权比较，优先保留较小的正权边。

#### 关键步骤  
1. 计算所有正权边的总和 $S$  
2. 构造最小生成树，边权取 $\max(C_i, 0)$  
3. 总奖励为 $S$ 减去生成树中正权边的权值和  

---

## 阻塞的道路

<br>


### 题意

给定一张 $n$ 个点 $m$ 条边的简单有向图，对于 $1 \leq i \leq m$ 依次回答：若在图上删除第 $i$ 条边，从 $1$ 至 $n$ 的最短路长度。


### 解法

一个朴素的想法就是每次询问都跑一遍 BFS，时间复杂度 $O((n+m)m)$，又因为在稠密图下，$O(m) = O(n^2)$，复杂度与 $O(n^4)$ 同阶。

---

BFS 求最短路复杂度已经无可挑剔，只能减少跑 BFS 的次数了。

最短路经过的边数总是不多于 $n-1$ 的，最终会改变最短路长度的边最多只有 $n-1$ 条边，其他边删了根本不会影响 $1$ 到 $n$ 最短路的长度。

所以我们可以直接先处理初始时 $1$ 到 $n$ 最短路上会经过哪些边与最短路长度，询问时如果删的边不在最短路路径内，那么最短路长度根本不会改变，直接输出全局最短路长度即可。如果在最短路路径内，就说明最短路长度可能会进行改变，此时再进行 BFS。

经过上述优化后，BFS 最多进行 $n$ 轮，所以最后复杂度为 $O((n+m)n)$。