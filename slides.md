---
# You can also start simply with 'default'
theme: default
# random image from a curated Unsplash collection by Anthony
# like them? see https://unsplash.com/collections/94734566/slidev
# some information about your slides (markdown enabled)
title: "讲题"
# apply unocss classes to the current slide
class: text-center

# https://sli.dev/features/drawing
drawings:
  persist: false
# slide transition: https://sli.dev/guide/animations.html#slide-transitions
transition: slide-left
# enable MDC Syntax: https://sli.dev/features/mdc
mdc: true
---

# 0207 讲题

---

## 地震震级尺度

<br>


### 题意

地震的震级是表示地震能量的对数尺度，震级每增加1，地震能量大约增加32倍。已知震级为$A$和$B$的地震，求震级为$A$的地震能量是震级为$B$的地震能量的多少倍。


### 解法

地震能量随着震级的增加成指数增长。根据题目条件，每增加1级，地震的能量会增加32倍。因此，如果震级$A$大于震级$B$，震级差$A - B$的能量就是$32^{(A - B)}$倍。

### 关键步骤

1. 计算震级差$level\_diff = A - B$。
2. 利用公式$result = 32 ^ {level\_diff}$，即震级差每增加1，能量就乘以32。
3. 输出结果。

---

## 字符串相等

<br>


### 题意

给定两个字符串 $S$ 和 $T$，判断是否可以通过至多一次交换 $S$ 中的两个相邻字符，使得 $S$ 和 $T$ 相等。如果能通过该操作变得相等，输出 "Yes"，否则输出 "No"。


### 解法

我们可以通过以下步骤来思考这个问题：

1. 直接比较：首先，如果字符串 $S$ 和 $T$ 已经相等，我们可以直接输出 "Yes"。
2. 查找不同的位置：如果字符串不相等，我们需要检查这两个字符串的不同字符的位置。仅允许有恰好两个不同的位置，且这两个位置必须是相邻的。
3. 相邻字符交换：如果两个不同的位置是相邻的，并且交换这两个位置的字符后，$S$ 和 $T$ 能变得相等，则返回 "Yes"。否则返回 "No"。

---

## 选择乘法

<br>

### 题意

给定一个整数 $N$，任务是将 $N$ 的数字重新排列并分割成两个正整数，目的是使这两个正整数的乘积最大。需要注意的是，分割后的整数不能包含前导零。

### 解法

直接暴力枚举所有的可能即可，时间复杂度$9! \times 9$。

首先，我们把输入的数字看成一个字符串，首先可以对它进行一个全排列，找出所有的排列的可能。

对于每种排列，我们都可以枚举切断的位置，判断没有前导0，且两个数字都不是0就可以计算两个部分的乘积，最后取一个最大值就可以。

---

## DNA序列

<br>

### 题意

给定一个由字符 $A$, $T$, $C$, $G$ 组成的字符串 $S$，要求找出满足以下条件的所有非空连续子串 $T$ 的数量：存在一个与 $T$ 互补的排列。具体来说，字符 $A$ 和 $T$ 互补，$C$ 和 $G$ 互补。子串的排列顺序可以改变，但字符的互补关系必须满足。


### 解法

根据题意可以推断出满足条件得子串必须A和T的数量相同,C和G的数量相同。

把A看作1，T看作-1，C和G看作0不管,那么满足条件的子串一定和为0。
把C看作1，G看作-1，A和T看作0不管,那么满足条件的子串一定和为0。

当然，实际上需要同时满足上面两个条件才行。

预处理出AT看作1和-1的前缀和数组sum1[]和CG看作1和-1的前缀额和数组sum2[],$O(n^2)$枚举区间,如果区间的两个区间和都是0,那么满足条件。

---

## 两个数组之间

<br>


### 题意

给定两个单调不下降的序列 `a`，`b`，求单调不下降序列 `c`，满足 $a_i \leq c_i \leq b_i$ 并且最长的数量。对 $998244353$ 取模。

$1 \leq n \leq 3000$，$0 \leq a_i \leq b_i \leq 3000$。

---

### 解法

考虑 dp。

我们定义一个 DP 数组 `f[i][j]`，表示的是：在前 `i` 个元素中，构造一个广义单调递增序列，并且序列最后一个数字不大于 `j` 的方法数。

#### 状态转移分析

1. **初始条件：**
   初始化 `f[1][x]`，这表示在只考虑序列的第一个元素时，以 x 结尾的单元素序列的数量。由于第一个元素可以从 `a[1]` 到 `b[1]` 任意取值，所以每个可能的值 $x(a_1 \leq x \leq b_1)$，都有 `x - a[1] + 1`种方式构造这样的序列。

2. **状态转移方程：**
    首先遍历$i$从$2$到$n$，对于每一个$i$，遍历$j$从$a_i$到$b_i$。

   按照$c_i$是不是$j$来进行分类。如果是$j$，则$c_{i-1}$最大只能是$j$，也就是说是前$i-1$项最后一个数字小于等于$j$的方案数，因此就是`f[i-1][j]`。如果当前这一项不是$j$,则就是小于等于$j$，也就是说，是用了$i$项，并且最后一个数字小于等于$j-1$(小于$j$)，也就是`f[i][j-1]`。

---

   因此可以得到`f[i][j] = f[i][j-1] + f[i-1][j]`，是这两项的和。
   
   但是我们又要知道，c的第$i-1$项还需要满足不小于$b_{i-1}$，我们遍历的时候也是从$a_{i-1}$到$b_{i-1}$,因此当$j>b_{i-1}$时，`f[i-1][j]`是可能为0的（因为在计算`f[i-1][j]`的时候，只会枚举最大到`b[i-1]`，如果直接用`f[i-1][j]`，而$b_{i-1} \leq j$，则会发生之前根本没有枚举`f[i-1][j]`这种情况，因此需要使用`f[i-1][min(j,b[i-1])]`。 

   (当然，你也可以选择把前面对于所有的$i$，$f[i][ai]$到$f[i][bn]$(注意不是$b_i$)都填充，这里就不用取$\min$了。)


由此得出程序：

```cpp
f[i][j] = (f[i][j-1] + f[i-1][min(j,b[i-1])]) % mod;
```

最后的答案即为 $f_{n,b_n}$。



---

## LEQ

<br>


### 题意

给定一个长度为 $N$ 的整数序列 $A = (A_1, A_2, \dots, A_N)$，需要找出满足以下条件的子序列 $A' = (A'_1, A'_2, \dots, A'_k)$ 的数量，且子序列长度至少为 2：

$A'_1 \leq A'_k$。
即找出所有满足首项小于等于末项的非连续子序列的数量，并对结果取模 $998244353$。

### 解法

固定头$a_i$与固定尾$a_j$，一共会有$2^{j-i-1}$个子序列，因为中间的都只有两种选择：选，或者不选，中间的数一共有$j-i-1$个，因此共有$2^{j-i-1}$次选择。那么我们就可以枚举序列的末端，然后再去枚举序列的头，公式为

$$
\sum_{j=1}^n \sum_{i=1}^{j-1} 2 ^{j-i-1} (a_i \leq a_j)
$$

---

上述公式实现起来的复杂度为$O(n^2)$,这明显会T.

对于每个$j$，我们要计算

$$
\sum_{i=1}^{j-1} 2 ^{j-i-1} (a_i \leq a_j)，
$$

式子变形一下，可以把$2^{j-1}$提出来，我们要计算的就是：

$$
2^{j-1} \sum_{i=1}^{j-1} 2^{-i} (a_i \leq a_j)
$$

这意味着我们需要**统计所有满足 $a_i \leq a_j$ 的元素，并对它们的贡献值进行累加**。如果每次都直接进行这种查询的话，会非常慢，时间复杂度是 $O(n^2)$。

---

树状数组帮助我们高效地维护和查询前缀和。具体来说：
- 我们将 **离散化后的索引**（而不是原数组值 $a[i]$）作为树状数组的索引。
- 每次处理一个新的 $a_j$，我们需要查找满足条件 $a_i \leq a_j$ 的元素的贡献。

通过树状数组，我们能够**高效地查询**小于等于 $a_j$ 的所有 $a_i$ 元素的累计贡献。假设在树状数组中维护的是 **每个离散化后的值的贡献**（即 $2^{-i}$ 的累积值），那么我们就可以通过查询来得到所有 $a_i \leq a_j$ 的贡献总和。
  
这样后面一个求和就可以从$O(n)$优化到$O(\log n)$。

由于数字本身比较大，会使得树状数组长度很大（$10^9$），因此需要对数据进行离散化，事实上我们只需要知道他们的大小关系，并不关心他们的实际大小，因此，我们可以对原来的数据进行一个排序，将新的数据映射为排序后的下标即可。例如$100,300,200$，我们可以映射为$1,3,2$。

---

但是，题目中要求的是对998244353取模运算的结果，

首先，我们需要一个快速幂函数来计算并保存 $2^{j-1}$，因为在每次计算贡献时都需要此值。另外，我们还需要计算 $2^{-i}$，即 $2$ 的负次幂，这可以通过计算 $2$ 的模逆元实现。

使用费马小定理，我们可以得到$2^{-i} \equiv 2^{p-1-i} \pmod{p}$,例如 $2^{-1} \mod 998244353 = 2^{998244351} \mod 998244353$。


```cpp
ll modInverse(ll base, ll m) {
    return qpow(base, m - 2, m); 
}
```








